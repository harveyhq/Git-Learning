/**
 * Test function
 *
 * @param {String} name
 * @returns Uppercased string
 */
function test(name) {
    return name === typeof String && name.toUpperCase();
}

// Run
console.log(test("dEmO tExT"));
